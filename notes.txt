================================================================================
LOW LEVEL C++ INTERVIEW PREP NOTES
================================================================================

================================================================================
ABOUT ME
================================================================================
- Self taught, 3 years as Software Engineer Associate at Lockheed Martin
- Strong intuition from real experience but often lack formal terminology
- Learned a lot by doing but no one explained the "why" or named the concepts
- Very logical, engineering minded — plan before building
- Learn best in context, through projects
- Tend to overload my plate — keep scope tight
- Love mathematics — dream is to be a cyber-physical systems engineer
- Recently doing self study: Codecademy (learned templates recently)

SKILLS I HAVE:
- OOP (solid)
- Pointers (conceptual understanding, deepened through this project)
- Heap vs stack (understand it, just needed terminology)
- Memory leaks, dangling pointers, RAII (knew concepts, now have the terms)
- Templates (recently refreshed)
- CMake (had used it before, forgot the workflow — now solid)
- Mutex / race conditions (used at Lockheed, now understand formally)
- Intuition for embedded constraints from Lockheed experience
- Saw: beamformer, tracker subsystems at Lockheed (now understand what they are)

GAPS TO FILL:
- Formal terminology for concepts I already understand intuitively
- Big O notation (intro done, need more practice)
- CPU registers and architecture (32 bit vs 64 bit) — on roadmap
- Bit manipulation — on roadmap
- Concurrency / mutex in depth — on roadmap
- Performance optimization — on roadmap
- const keyword placement — confusing, needs dedicated session
- State machines — critical for embedded, on roadmap

================================================================================
INTERVIEW FOCUS AREAS — RAYTHEON SOFTWARE EFFECTORS ROLE
================================================================================
Company: Raytheon
Role: Software effectors on missile systems
Interview: June 30th at 2:00pm (one week away as of June 23rd)
Format: 2 panelists, NO coding or whiteboarding — all verbal
Decision: within 24-48 hours

PANELIST 1 — OOP and C++:
- OOP fundamentals
- C++ knowledge
- Any exposure to hardware/software integration

PANELIST 2 — Real-time embedded:
- Real-time embedded experience
- Scheduling
- Priority inversion (screener name-dropped this — know it cold)
- Behavioral questions (use Claude desktop + Lockheed experience for these)

PREP STRATEGY:
- Code it AND talk about it — not just answer in head, explain out loud
- No whiteboarding so verbal clarity is everything
- C++ and OOP is review/reinforcement from Codecademy + this project
- Real-time embedded is the gap to close most urgently
- Behavioral: handled separately with Claude desktop + resume

STUDY FOCUS:
1. C++ fundamentals — pointers, memory, OOP (in progress via project)
2. Real-time concepts — determinism, scheduling, task priorities, deadlines
3. RTOS — what it is, how it schedules tasks
4. Priority inversion — know it cold, both solutions
5. Hardware/software integration — interrupts, memory mapped I/O basics
6. Safety mindset — failure modes, what happens when things go wrong
7. State machines — everywhere in embedded systems

FULL STUDY LIST (hit all before June 30th):

  C++ & OOP:
  - LinkedList Rule of Three (in progress)
  - const keyword placement
  - Virtual functions / polymorphism
  - Hardware/software integration basics

  Real-Time Embedded & Concurrency:
  - Mutex in depth — locking, unlocking, deadlock
  - Concurrency — threads, shared data
  - RTOS deep dive
  - Interrupts and interrupt handlers
  - Memory mapped I/O
  - State machines
  - Priority inversion (verbal drill — know it cold)

  Data Structures (project):
  - LinkedList complete
  - Queue or Stack
  - Bit flags / bit manipulation
  - Thread safe data structure

  Performance:
  - Big O in practice
  - AddressSanitizer
  - Amortized analysis

  Practice:
  - Verbal answers to all 15 interview questions
  - Mock panel Q&A session before the interview

================================================================================
PROJECT: EMBEDDED DATA STRUCTURES LIBRARY
================================================================================
GitHub: github.com/GlitchLabs/embedded-data-structures
Branch strategy: feature branches -> PR -> merge to main

GOAL: Build a reusable data structures library without STL, from scratch
      Simulates what real embedded engineers actually write

ROADMAP:
  1. Dynamic Array    [COMPLETE] — memory, pointers, templates, Rule of Three
  2. Linked List      [IN PROGRESS] — nodes, traversal, Rule of Three
  3. Queue or Stack   — built on linked list, connects to task scheduling
  4. Bit flags        — bit manipulation
  5. Thread safety    — mutex, concurrency, priority inversion in practice
  6. Profiling        — performance, Big O in practice, AddressSanitizer

LEARNING STRUCTURE:
  - After each concept: explain it back in own words
  - After each feature: small coding challenge without help
  - Every few topics: mini review session from notes

================================================================================
TOOLS & ENVIRONMENT
================================================================================

CMAKE:
- CMake is a build system GENERATOR — it does NOT compile code
- Reads CMakeLists.txt and generates instructions (Makefiles) for Make to follow
- Analogy: CMake is the ARCHITECT, Make is the CARPENTER

CMAKE WORKFLOW:
  mkdir build
  cd build
  cmake ..    <- points CMake at parent directory where CMakeLists.txt lives
  make        <- actually compiles and links

WHEN TO RUN cmake .. vs make:
- cmake ..: only when project STRUCTURE changes (new files, new dirs, CMakeLists.txt)
- make: anytime you change code inside existing files
- INCREMENTAL BUILDS: Make only recompiles files that changed

CMakeLists.txt STRUCTURE:
  cmake_minimum_required(VERSION 3.10)
  project(projectName)
  add_executable(targetName source.cpp)
  target_include_directories(targetName PRIVATE folderName)

  - add_executable MUST come before target_include_directories
  - ACCESS LEVELS: PRIVATE (this target only), PUBLIC (this + dependents), INTERFACE (dependents only)

HOW #include WORKS:
- #include = copy-pasting the header into the file that includes it
- Compiler ONLY sees files pulled in via #include or add_executable
- Header sitting on disk is invisible to compiler until included
- Syntax error in header won't show until something includes it

COMPILER vs IDE — HOW THEY FIND HEADERS:
- Two separate systems — compiler uses CMakeLists.txt, IDE has its own system
- IDE include error does NOT mean code won't compile — always check make first
- Fix: CMake Tools extension in VS Code + run CMake: Configure

INTELLISENSE (VS Code):
- Powers autocomplete, parameter hints, error highlighting, go-to-definition
- Needs to be synced with CMake — run CMake: Configure or reset IntelliSense database
- Can disable inline suggestions while learning (good for building recall)

================================================================================
GIT & GITHUB
================================================================================

THREE STEP WORKFLOW:
  git add <files>     <- stage specific files (be explicit, not git add .)
  git commit -m ""    <- commit with message
  git push            <- push to remote

COMMIT MESSAGE CONVENTION:
  First line: short summary, under 50 chars, imperative tense ("Add" not "Added")
  Blank line + body if needed

ATOMIC COMMITS: each commit = one logical change

.gitignore: commit this file — teammates need it too

BRANCH COMMANDS:
  git switch -c <branch>    <- create and switch (-c = create)
  git switch <branch>       <- switch to existing branch
  git push -u origin <branch>  <- push new branch (-u sets tracking, first time only)
  git branch                <- list branches

STRATEGY: Never commit to main directly — always feature branch -> PR -> merge

================================================================================
MEMORY MANAGEMENT
================================================================================

STACK vs HEAP:
- Stack: automatic, local variables and function calls, fixed size, freed on scope exit
  In embedded: blowing the stack (deep recursion, large locals) = crash, no OS to save you
- Heap: intentional, requested with new, YOU must free with delete
  Unpredictable timing — bad for real-time systems

ALLOCATING & FREEING:
  new T[n]    <- allocate array on heap, returns address of first element
  delete[] p  <- free array heap memory (runs destructor on each element)
  delete p    <- free single object heap memory
  ALWAYS match: new[] with delete[], new with delete — compiler won't catch mismatches

WHAT delete[] ACTUALLY DOES:
- Runs destructor on each element (if T has one)
- Returns memory to heap — marks it available again
- Does NOT touch the pointer — pointer still holds old address (dangling pointer risk)

DELETE ORDER MATTERS:
  Wrong: m_data = newData;  then  delete[] newData;  <- deletes what you just assigned!
  Right: delete[] m_data;   then  m_data = newData;  <- free old first, then reassign

WHAT A POINTER ACTUALLY IS:
- Just a number — a memory address in hexadecimal (e.g. 0x7f8b4c002a00)
- m_size = 4 (the number IS the value), m_data = 0xAAA (the number is a LOCATION)
- "The map, not the territory"
- On 64 bit system: pointer is 8 bytes regardless of what it points to

WHY POINTERS ARE FAST:
- Passing large object by value = copying every byte
- Passing by pointer/reference = copying one 8-byte address
- Always pass large objects by const reference: const T& not T

POINTER ARITHMETIC:
  array[i]  ==  *(array + i)  — identical, compiler does: base + (i * sizeof(T))
  Arrays must be contiguous — the math only works if elements are adjacent

LOCAL POINTER VARIABLES — no cleanup needed:
- Local variables live on the stack, destroyed automatically when scope ends
- Only delete heap memory (new), only nullptr member variables accessed after deletion
- Rule: stack takes care of itself, heap is YOUR responsibility

HOTEL ANALOGY:
- new = checking in, heap gives you a room number
- delete = checking out, room available again but number still exists in your hand
- Dangling pointer = holding a room number to a room that's been given to someone else

HEAP FRAGMENTATION:
- Long-running systems fragment heap: [used][free][used][free]
- Dynamic array CANNOT use fragmented memory — needs contiguous block
- Linked list CAN — each small node fits anywhere
- Embedded solution: static allocation (fixed buffers at compile time)

================================================================================
MEMORY MANAGEMENT ERRORS
================================================================================

MEMORY LEAK:
- Heap memory allocated but never freed
- Desktop: OS reclaims on exit. Bare metal embedded: gone until power cycle
- On a missile running 24/7: mission critical — a leak = eventual system failure
- Every new MUST have a matching delete

DANGLING POINTER:
- Pointer holding address to memory you no longer own (freed)
- Causes: deleting without nulling pointer, shallow copy + destructor
- Reading/writing through it = undefined behavior
- Fix: set to nullptr after deleting (if not immediately reassigning)

DOUBLE FREE:
- Calling delete on same memory block twice
- Caused by shallow copy — two objects pointing at same heap memory, both destructors run
- Result: undefined behavior, crash, silent corruption

SHALLOW COPY (leads to dangling pointer + double free):
- Default compiler copy copies pointer ADDRESS not heap DATA
- Two objects point at same block
- Destructor of one: other gets dangling pointer
- Destructor of both: double free

UNDEFINED BEHAVIOR:
- C++ standard doesn't define what happens — anything can occur
- Common causes: dangling pointer, double free, out of bounds, uninitialized vars
- Compiler will NOT always catch it
- In embedded: can corrupt other subsystem data, cause silent wrong behavior

OFF BY ONE:
- m_size = 3: valid indexes 0, 1, 2
- i < m_size  ✓    i <= m_size  ✗ (reads memory you don't own)

================================================================================
C++ CONCEPTS
================================================================================

RAII (Resource Acquisition Is Initialization):
- Tie resource lifetime to object lifetime
- Destructor cleans up automatically when object goes out of scope
- Prevents memory leaks

NULLPTR:
- Explicit "this pointer points at nothing" — always use instead of leaving uninitialized

SIZE_T:
- Unsigned integer for sizes and counts — can't be negative
- Matches platform size automatically (32 bit = 32 bit size_t)
- Lives in <cstddef>. Use for: size, capacity, indices, loop counters

FIXED WIDTH TYPES (<cstdint>):
- uint32_t, uint16_t, int8_t — guaranteed size regardless of platform
- Critical in embedded where hardware varies and resources are scarce

DECLARATION vs DEFINITION:
- Declaration: "this thing exists, here's its name and shape"
- Definition: the actual implementation, the body

TEMPLATES:
- Allow class/function to work with any type: template<typename T>
- typename and class are interchangeable (typename is more modern)
- DynamicArray<T> alone is invalid — T unknown, can't compile
- DynamicArray<int> triggers compiler to generate a concrete class for int
- Both declaration AND definition go in header — compiler needs full definition visible
- Regular classes can split .hpp/.cpp — templates cannot

FOUR PILLARS OF OOP:

  ENCAPSULATION:
  - Bundle data and methods together, hide implementation details
  - Private member variables, public interface
  - User can use the object without knowing how it's implemented
  - Changing implementation should not affect the interface

  ABSTRACTION:
  - Hide complexity — expose only what's necessary
  - Not HOW it works, just WHAT it does
  - Related to encapsulation but distinct:
    Encapsulation = bundling + hiding data
    Abstraction = hiding complexity behind a simple interface

  INHERITANCE:
  - Child class inherits data and behavior from parent class
  - Example: Shape -> Square, Vehicle -> Car/Truck/Boat
  - Child gets parent's methods and data, can add or override

  POLYMORPHISM:
  - One interface, many implementations
  - A Vehicle* pointer can point at Car, Truck, or Boat
  - Call the same method on each — each responds differently
  - Key for writing flexible, extensible code
  - In embedded: one Subsystem* can point at Guidance, Navigation, Control

VIRTUAL FUNCTIONS:
  virtual void update()       <- overridable, base class CAN have implementation
  virtual void update() = 0   <- pure virtual, derived class MUST implement it
                                 makes the class ABSTRACT — cannot be instantiated

ABSTRACT vs CONCRETE CLASS:
- Abstract: has at least one pure virtual function, cannot be instantiated
  Defines the interface that all derived classes must follow
- Concrete: fully implemented, no pure virtual functions, CAN be instantiated

VIRTUAL DESTRUCTOR:
- If a class has virtual functions, its destructor should also be virtual
- WHY: if you delete a derived object through a base pointer without virtual destructor,
  only the base destructor runs — derived class memory leaks
- Rule: if a class is meant to be inherited from, make destructor virtual

const char* vs std::string IN EMBEDDED:
- std::string uses dynamic allocation internally — avoid in embedded
- const char* is a pointer to a string literal — no heap allocation, deterministic
- Use const char* for strings in embedded/real-time contexts

IN-CLASS MEMBER INITIALIZATION (C++11):
  Node<T>* next = nullptr;  <- set at declaration
- Initializer list takes precedence if both present
- Good for simple defaults

DOT vs ARROW OPERATOR:
- . (dot): access member through object
- -> (arrow): access member through pointer — shorthand for (*ptr).member
- Linked lists use -> everywhere — nodes are always pointers

OPERATOR OVERLOADING:
- Redefine what operators do for custom types
- operator=  (copy assignment), operator[]  (subscript), operator+  (addition)

'this' POINTER:
- Pointer to the object the method was called on
- *this = the object itself
- Used in copy assignment: return *this

LAZY vs EAGER INITIALIZATION:
- Lazy: don't allocate until needed (right for embedded — scarce resources)
- Eager: allocate upfront (convenient but wasteful)

CONST KEYWORD PLACEMENT:
  const T* p       // pointer to const T — can't change value, CAN move pointer
  T* const p       // const pointer to T — CAN change value, can't move pointer
  const T* const p // can't change either

  Trick: left of * = about the value. Right of * = about the pointer itself.

  Three places const appears on functions:
  1. const parameter:       void read(const uint32_t* data)   — won't modify what's passed in
  2. const member function: uint32_t get() const              — won't modify object's members (use on all getters)
  3. const return type:     const uint32_t* getBuffer() const — caller gets read-only access

ASSERT:
  #include <cassert>
  assert(condition);  <- halts program if false, reports location
- Debug builds: active. Release builds (NDEBUG defined): compiles to nothing
- Use for: programmer errors, states that should never happen
- Embedded: use assert in debug, error codes in production, never exceptions

EXCEPTIONS IN EMBEDDED:
- Stack unwinding = unpredictable timing = breaks determinism
- Many embedded compilers disable exceptions entirely
- Rule: exceptions are off the table in embedded

TERNARY OPERATOR:
  condition ? value_if_true : value_if_false
  m_capacity = (m_capacity == 0) ? 1 : m_capacity * 2;

================================================================================
OOP & CLASS DESIGN
================================================================================

RULE OF THREE (C++03):
If you manage raw memory and define ANY of these, define ALL THREE:
1. Destructor          — free heap memory when object goes out of scope (~ClassName)
2. Copy constructor    — deep copy when creating new object from existing
3. Copy assignment     — deep copy when overwriting existing object (operator=)
WHY: compiler-generated versions do shallow copy = dangling pointer + double free

RULE OF FIVE (C++11+):
Adds to Rule of Three:
4. Move constructor   — transfer ownership (DynamicArray<T>&& other)
5. Move assignment    — transfer ownership on assignment
Move = hand over the pointer, don't copy. Much more efficient.
We write embedded-style C++ — Rule of Three first, move semantics later.

DESTRUCTOR:
- Runs automatically when object goes out of scope
- Syntax: ~ClassName() { }
- Linked list destructor must traverse and delete each node individually
  (each node is a separate heap allocation)
  Pattern: save next, delete current, advance
    while(m_head != nullptr) {
        Node<T>* next = m_head->next;
        delete m_head;
        m_head = next;
    }

COPY CONSTRUCTOR:
- Triggered when: DynamicArray<int> array2 = array1; (array2 is new)
- Signature: DynamicArray(const DynamicArray<T>& other)
- Must do deep copy — allocate new memory, copy data

COPY ASSIGNMENT OPERATOR:
- Triggered when: array2 = array1; (array2 already exists)
- Signature: DynamicArray<T>& operator=(const DynamicArray<T>& other)
- Returns *this to allow chaining (a = b = c)
- Extra steps: self-assignment guard + free existing memory first
  Steps: guard -> delete existing -> copy size/capacity -> allocate -> copy data -> return *this
- Self-assignment guard: if(this == &other) return *this;

ENCAPSULATION:
- Keep member variables private
- Expose behavior through public methods, not raw internals
- Getters for read access (size()), no direct pointer exposure

================================================================================
DATA STRUCTURES
================================================================================

DYNAMIC ARRAY:
Member variables: T* m_data, size_t m_size, size_t m_capacity
Growth strategy (doubling):
  1. Check if size == capacity
  2. New capacity = (capacity == 0) ? 1 : capacity * 2
  3. Allocate new heap block
  4. Copy old data
  5. Delete old block
  6. Point m_data at new block
  7. Add element, increment size
Big O: lookup O(1), append amortized O(1), insert middle O(n), growth O(n)

LINKED LIST:
Node struct: T value, Node<T>* next = nullptr
Member variables: Node<T>* m_head, Node<T>* m_tail, size_t m_size
- m_tail: O(1) append (without it: O(n) to find end)
- Use -> to access node members (nodes are always pointers)

Dynamic Array vs Linked List:
  Dynamic Array: O(1) lookup, O(n) insert/delete middle, needs contiguous memory
  Linked List:   O(n) lookup, O(1) insert/delete (with pointer), scattered memory OK
  Linked list advantage in embedded: works with fragmented heap

================================================================================
REAL TIME EMBEDDED SYSTEMS
================================================================================

HARDWARE/SOFTWARE INTEGRATION:
- Matching software to hardware constraints: RAM limits, timing, determinism
- Software engineers read DATASHEETS — hardware documents that define memory maps,
  register locations, timing specs, and pin assignments

CPU REGISTERS:
- Tiny storage locations inside the CPU itself — not RAM
- CPU cannot do math directly on RAM — it loads values into registers first
- counter++ is actually: load from RAM into register, add 1, store back to RAM
- That 3-step sequence is the race condition window in concurrency
- Hardware chips (sensors, actuators) also have their own registers on the chip

MEMORY MAPPED I/O:
- How software talks to hardware directly — no drivers in the traditional sense
- Hardware registers (sensors, actuators, peripherals) are assigned fixed memory addresses
- Software reads/writes those addresses like normal memory — CPU routes the request
- Example: uint32_t* sensor = (uint32_t*)0x40001000; uint32_t val = *sensor;
- That address isn't RAM — it's the sensor chip itself
- C++ makes this possible because it allows raw pointer access to any memory address
- Higher level languages (Python) hide this — C/C++ dominate embedded for this reason

SENSORS, ACTUATORS, REGISTERS:
- Sensors: hardware that reads the physical world — accelerometer, gyroscope, GPS, temperature
  Physical stimulus -> electrical signal -> chip converts to number -> stores in register
- Actuators: hardware your software commands to do something physical
  Fins/control surfaces (steering), fuze (detonation), throttle (thrust)
- Registers: tiny storage on the chip itself — hold current readings or control chip behavior

THE BUS:
- Wires on the circuit board connecting the CPU to RAM, sensors, GPU, storage
- CPU sends a request over the bus to a memory address
- The bus routes that request to the correct chip based on the address range
- Common embedded buses: SPI, I2C, UART
- PCIe: faster modern bus for GPUs, NVMe drives on general purpose computers

THE MEMORY MAP:
- An agreement defined by hardware engineers when designing the board
- Assigns address ranges to specific hardware: RAM here, flash here, sensors here
- Physically baked into the board — bus traces route addresses to the right chips
- Software engineer reads the datasheet, writes code that uses the correct addresses
- That's hardware/software integration: reading the hardware design, writing software to match it
- Analogy: addresses are the agreement, bus traces are the roads, chips are the buildings

EMBEDDED:
- Software on dedicated hardware with specific purpose
- Often: limited RAM, no hard drive, sometimes no OS (bare metal)
- Examples: microcontrollers, FPGAs, guidance systems

REAL TIME:
- Timing guarantees — operations MUST complete within X microseconds, every time
- Missing a deadline = correctness failure, not just performance problem
- KEY TERM: DETERMINISM — not just fast, predictably fast

FIRMWARE:
- Real software burned onto hardware, runs without OS
- "Burned" from old ROM chip days — now uses flash memory (erasable)
- Firmware engineers write C and C++ — real code, real bugs

WHY STL IS AVOIDED IN EMBEDDED:
- Dynamic allocation has unpredictable timing — breaks determinism
- STL overhead too large for resource constrained hardware
- Solution: roll your own — know exactly what code does and how long it takes

MEMORY vs RAM:
- RAM: physical hardware, stores data while running (where stack and heap live)
- Memory: broader — RAM, flash, ROM, cache, registers
- "Scarce resources" in embedded usually means RAM

STATIC ALLOCATION (to learn later):
- Fixed size buffers at compile time, no dynamic allocation
- Avoids fragmentation entirely — common in hard real-time systems

EMBEDDED ENGINEER HABITS — RULES TO LIVE BY:
1. No dynamic allocation at runtime — no new, delete, std::string, std::vector
   Allocate everything at startup or compile time. Unpredictable timing breaks determinism.
2. No exceptions — stack unwinding is unpredictable. Use error codes, assert, or status flags.
3. Prefer static allocation — fixed-size arrays instead of dynamic containers.
   You know the size at compile time, so lock it in.
4. Volatile on hardware-facing variables — tells compiler not to optimize away variables
   that hardware can change outside your code's control. (covered later)
5. Keep ISRs short — Interrupt Service Routines must be fast.
   No blocking, no mutexes, no heavy logic. Set a flag and get out.
6. No blocking in real-time tasks — if a task blocks waiting for something, it misses its deadline.
7. Know your stack depth — deep recursion can blow the stack on embedded. No OS to save you.

STRING LITERALS IN EMBEDDED:
- "IDLE", "ARMED" etc. are burned into the binary at compile time in read-only data (.rodata)
- const char* points to that fixed location — no heap allocation, completely deterministic
- std::string copies data to the heap on construction — never use in real-time context
- Rule: use const char* for strings in embedded, never std::string

================================================================================
REAL-TIME SCHEDULING & CONCURRENCY
================================================================================

RTOS (Real-Time Operating System):
- Schedules tasks with timing guarantees
- Every task has a PRIORITY — scheduler runs highest priority READY task
- Task states: running, ready, blocked, sleeping

TASK PRIORITIES (missile system example):
- High:   Guidance — must run every few ms, life or death
- Medium: Telemetry — sending data to operators
- Low:    Logging — recording events

THE FEUDAL JAPAN ANALOGY — THREADING AND PARALLELISM:
- God          — hardware itself. Acts of God are interrupts — bypass every
                 layer, tap the CPU directly, cannot be ignored
- Emperor      — the OS. Supreme law of the realm, manages the whole system
- Shogun       — the scheduler. Real day-to-day power, distributes work across
                 cores, decides who gets CPU time
- Daimyo       — CPU cores. Regional lords. Multiple Daimyo run simultaneously —
                 that's true parallelism. Each manages their own domain.
- Samurai      — threads. Workers executing tasks under each Daimyo. Only one
                 samurai fights at a time per Daimyo — context switching swaps them
- The realm    — memory. Shared land all Daimyo can access but must coordinate
                 over. Mutexes are the laws governing who uses which territory.

Single core = one Daimyo, many samurai taking turns (concurrency, not parallelism)
Multi core  = many Daimyo running simultaneously (true parallelism)
Interrupt   = act of God, reaches down to a Daimyo directly regardless of what
              the Shogun has scheduled

PROCESS vs THREAD:
- Process: a running program — gets its own isolated memory space (heap, globals, resources)
- Thread: a path of execution INSIDE a process — multiple threads share the same memory
- CPU switches between threads so fast it feels simultaneous — called CONTEXT SWITCHING
- The OS decides who gets CPU time and when — that's the SCHEDULER
- Only one thread runs on one core at any given moment

MUTEX (Mutual Exclusion):
- Locks shared resource so only one task accesses it at a time
- Prevents RACE CONDITIONS — two tasks reading/writing same data simultaneously
- Task locks -> does work -> unlocks. Other tasks block until unlocked.
- You don't lock the variable — you lock the CRITICAL SECTION (the block of code that touches it)

RACE CONDITION:
- Two tasks access shared data simultaneously, result depends on timing
- Example: counter++ looks like 1 step but is actually 3: read, add 1, write back
- Scheduler can yank a thread between any of those steps — that's the race window
- Result can be silent wrong data (no crash, no error message)
- Fix: mutex around the critical section

DEADLOCK:
- Two or more threads permanently waiting on each other — none can proceed
- Two ways it happens:
  1. Thread crashes or never releases mutex — lock held forever, waiters blocked forever
  2. Circular dependency — Thread A holds mutex 1, waits on mutex 2
                           Thread B holds mutex 2, waits on mutex 1
                           Neither can proceed — they wait forever
- No crash required for deadlock — just permanent blocking
- On a missile: deadlocked guidance task = missed deadline = mission failure

MANUAL LOCK vs LOCK_GUARD:
- counterMutex.lock() / counterMutex.unlock() — manual, dangerous
- If anything goes wrong between lock and unlock (crash, assert, early return),
  unlock never gets called — mutex stays locked forever
- On general-purpose OS: process dies, OS cleans up all locks — no deadlock
- On bare metal embedded RTOS: no OS safety net — tasks waiting on that mutex
  wait forever, system stuck in partial state
- std::lock_guard is RAII for mutexes — guarantees unlock when scope exits
- Rule: always prefer lock_guard over manual lock/unlock

ATOMIC OPERATION:
- An operation that completes fully without any possibility of interruption
- counter++ is NOT atomic — it's 3 steps the scheduler can split
- std::atomic<int>: uses special CPU instructions to make single operations truly atomic
- std::mutex: software-level atomicity for complex multi-line critical sections
- Use std::atomic for simple single-variable operations
- Use std::mutex when critical section is multiple lines or complex logic

PRIORITY INVERSION:
- High priority task indirectly blocked by low priority task
- Scenario:
    1. Low priority locks mutex on shared sensor data
    2. High priority wakes up, needs mutex — BLOCKED
    3. Medium priority wakes up, doesn't need mutex — runs freely
    4. Medium runs while high waits on low = high effectively runs at low priority
- On a missile: guidance misses deadline = wrong course correction = mission failure

SOLUTIONS:
  Priority Inheritance (reactive):
  - Low priority task inherits high priority temporarily while holding mutex
  - Finishes fast, releases mutex, drops back to normal priority

  Priority Ceiling (proactive):
  - Mutex assigned ceiling = highest priority task that will ever use it
  - Any task locking mutex immediately runs at ceiling
  - Prevents inversion before it starts

MISSILE SOFTWARE ARCHITECTURE:
Each subsystem = its own RTOS task with priority
Communication: shared memory, message queues, publish-subscribe

- Guidance      — where am I, where going, what's my target
- Navigation    — fuses GPS/IMU sensor data, tracks position/velocity
- Control       — takes guidance commands, moves fins/actuators
- Seeker/Tracker— finds and tracks target (seen at Lockheed)
- Fuze          — determines when to detonate
- BIT           — Built-in Test, health monitoring
- Communication — datalink to operator

GUIDANCE SYSTEM:
- Brain of the missile
- Fuses: GPS, accelerometers, gyroscopes
- Calculates course corrections every few milliseconds
- Math: Kalman filters, PID controllers, quaternion math
- Missing deadline = wrong correction = mission failure

BEAMFORMER: radar/sonar signal processing, raw antenna signals mathematically focused
TRACKER: takes beamformer output, predicts target position, maintains track file

CYBER-PHYSICAL SYSTEMS:
- Where math and software meet physical systems
- The missile effectors role IS cyber-physical systems engineering
- Path forward: Kalman filters, PID controllers, control theory, DSP

DIAGRAMS FOR EMBEDDED:
- Block diagrams: subsystem connections
- Timing diagrams: when tasks run, deadlines
- State machines: system behavior modeling (critical — to cover)
  Example missile states: idle -> armed -> tracking -> terminal guidance -> detonation

================================================================================
BIG O NOTATION
================================================================================
- How an algorithm SCALES as input grows — time and space complexity

O(1)     — constant, doesn't scale with input (array index lookup, hash map get)
O(n)     — linear, scales with input (copying all elements, linked list search)
O(n²)   — quadratic, nested loops (bubble sort)
O(log n) — logarithmic, binary search, very efficient
Amortized O(1) — occasional expensive operation averaged over many cheap ones
                 (dynamic array growth — rare O(n) copies, mostly O(1) appends)

================================================================================
PROGRAM DESIGN & STRUCTURE
================================================================================
- Currently a learning gap — objects connecting without spaghetti code
- Recently learned: dependency injection (need more reps)

TO LEARN IN CONTEXT THROUGH PROJECT:
- Encapsulation: expose behavior not internals
- Dependency management: use objects inside objects without tight coupling
- Separation of concerns: new things shouldn't break existing things
- Dependency injection, SOLID principles, design patterns

================================================================================
INTERVIEW REVIEW QUESTIONS
================================================================================
Practice until answers are automatic.

C++ FUNDAMENTALS:
1.  What is a real-time embedded system and why does it matter for code?
    KEY: determinism, bare metal, timing guarantees, scarce resources, STL avoidance

2.  What is the Rule of Three and why does it exist?
    KEY: destructor + copy constructor + copy assignment, heap memory, shallow copy danger

3.  What is a dangling pointer and what causes one?
    KEY: freed memory, pointer still holds address, shallow copy, destructor

4.  What is the difference between delete and delete[]?
    KEY: single object vs array, destructor on first element only, undefined behavior

5.  Explain shallow copy vs deep copy and why it matters.
    KEY: copies address not data, dangling pointer, double free, new memory + copy loop

6.  What is assert and why use it instead of exceptions in embedded?
    KEY: stack unwinding, determinism, NDEBUG, compiles to nothing in release

7.  Walk through push_back grow sequence step by step.
    KEY: size==capacity, double capacity, allocate, copy, delete old, reassign pointer, add element, increment

8.  What is the difference between copy construction and copy assignment?
    KEY: new object vs existing, self-assignment guard, free existing memory first

9.  Why do templated classes put declaration and definition in the header?
    KEY: concrete class per type, full definition needed at use site, translation unit

10. What is CMake and what does Make do?
    KEY: build system generator, generates Makefiles, Make compiles/links, architect/carpenter

SCHEDULING & EMBEDDED:
11. What is priority inversion and why is it dangerous on a missile system?
    KEY: high blocked by low, medium runs freely, missed guidance deadline, mission failure

12. What are the two solutions to priority inversion?
    KEY: priority inheritance (reactive, temporary promotion), priority ceiling (proactive, preassigned)

13. What is a race condition and how do you prevent it?
    KEY: shared data + simultaneous access + timing dependent result, fix with mutex

14. What is an RTOS and how does it differ from a general OS?
    KEY: timing guarantees, task priorities, deterministic scheduling, real-time deadlines

15. Why is dynamic memory allocation dangerous in real-time embedded systems?
    KEY: unpredictable timing, heap fragmentation, determinism broken, STL avoidance

================================================================================
BIT MANIPULATION
================================================================================

WHY IT MATTERS IN EMBEDDED:
- Hardware registers are collections of bits — each bit controls or reports
  one specific thing
- You read one register and get all bits at once — you need to isolate the
  ones you care about
- You can only write to the whole register — you need to change specific bits
  without touching the others

NUMBER SYSTEMS:
- Binary (base 2): symbols 0 and 1. Prefix: 0b. Example: 0b10111101
- Hexadecimal (base 16): symbols 0-9 and A-F. Prefix: 0x. Example: 0xFF
- Each hex digit = exactly 4 bits. Two hex digits = one byte (8 bits)
- Computers use hex because it maps cleanly to binary — easier to read than
  32 ones and zeros

BITWISE OPERATORS:
  &   AND  — both bits must be 1 to get 1. Otherwise 0.
  |   OR   — at least one bit must be 1 to get 1. Both 0 gives 0.
  ~   NOT  — flips every bit. 0 becomes 1, 1 becomes 0.
  <<  left shift  — moves bits left N positions, fills right with 0
  >>  right shift — moves bits right N positions, fills left with 0

CREATING A MASK:
- Start with 1, shift left to the target bit position
- Example: target bit 3 → 1 << 3 → 0b00001000
- The shift creates a 1 at exactly the position you want

THREE OPERATIONS:

  CHECK a bit (is it on or off?):
  - AND register with mask — nonzero means bit is set, zero means it's not
  - Example: reg & (1 << 3)
  - Technique: masking

  SET a bit (force it to 1):
  - OR register with mask — OR with 1 forces bit on, OR with 0 leaves untouched
  - Example: reg | (1 << 5)

  CLEAR a bit (force it to 0):
  - AND register with inverted mask — NOT flips the mask so target bit is 0
    and everything else is 1. AND with 0 forces bit off, AND with 1 leaves untouched
  - Example: reg & ~(1 << 7)

PRINTING BITS IN C++:
  #include <bitset>
  std::cout << std::bitset<8>(value);  // prints 8 bits — use for debugging
  // Note: don't use in production embedded code — standard library overhead

  Manual (embedded-safe):
  for(int i = 7; i >= 0; i--) {
      std::cout << ((value >> i) & 1);
  }
  // Shifts each bit down to position 0, masks with & 1, prints 0 or 1

UINT8_T PRINTING GOTCHA:
- uint8_t is an alias for unsigned char
- std::cout treats it as a character, not a number — may print nothing or
  a control character
- Fix: cast to int before printing: std::cout << (int)value;

================================================================================
