================================================================================
LOW LEVEL C++ INTERVIEW PREP NOTES
================================================================================

================================================================================
ABOUT ME
================================================================================
- Self taught, 3 years as Software Engineer Associate at Lockheed Martin
- Strong intuition from real experience but often lack formal terminology
- Learned a lot by doing but no one explained the "why" or named the concepts
- Very logical, engineering minded — plan before building
- Learn best in context, through projects
- Tend to overload my plate — keep scope tight
- Recently doing self study: Codecademy (learned templates recently)

SKILLS I HAVE:
- OOP (solid)
- Pointers (conceptual understanding)
- Heap vs stack (understand it, just needed terminology)
- Memory leaks (knew the concept)
- RAII (knew the concept, forgot the acronym)
- Templates (recently refreshed)
- CMake (had used it before, forgot the workflow)
- Intuition for embedded constraints from Lockheed experience

GAPS TO FILL:
- Formal terminology for concepts I already understand intuitively
- Big O notation
- CPU registers and architecture (32 bit vs 64 bit)
- Bit manipulation
- Concurrency
- Performance optimization
- size_t and fixed width types (seen them, didn't fully understand them)

================================================================================
TOOLS & ENVIRONMENT
================================================================================

CMAKE:
- CMake is a build system GENERATOR — it does NOT compile your code
- It reads CMakeLists.txt and generates instructions (like Makefiles) for the
  actual build tool to follow
- Analogy: CMake is the ARCHITECT, Make is the CARPENTER
- Make reads CMake's generated Makefiles and does the actual compiling/linking

CMAKE WORKFLOW:
  mkdir build
  cd build
  cmake ..       <- points CMake at parent directory where CMakeLists.txt lives
  make           <- actually compiles and links your code

CMakeLists.txt STRUCTURE:
  cmake_minimum_required(VERSION 3.10)   <- minimum cmake version needed
  project(projectName)                   <- declares project name
  add_executable(targetName source.cpp)  <- what to build and from what files
  target_include_directories(targetName ACCESS_LEVEL folderName) <- where headers live

- add_executable must come BEFORE target_include_directories — CMake reads top to bottom
  the target must exist before you can configure it

target_include_directories ACCESS LEVELS:
  PRIVATE   — only this target needs these headers (use this for executables)
  PUBLIC    — this target AND anything linking to it gets the headers
  INTERFACE — this target doesn't need them but anything linking to it does

- Build directory keeps generated files separate from source files (clean)
- To add a new .cpp file: add it to add_executable(), then re-run cmake .. and make
- Installed version: 3.10 is a safe minimum (system has 3.31.0)

WHEN TO RUN cmake .. vs make:
- cmake ..: only when project STRUCTURE changes
    adding new source files
    adding new include directories
    changing CMakeLists.txt
- make: anytime you change code inside existing files
- Day to day workflow: just make then ./LLCPP
- INCREMENTAL BUILDS: Make only recompiles files that changed — smart and efficient
  If main.cpp didn't change, Make skips it entirely

================================================================================
C++ CONCEPTS
================================================================================

HOW #include WORKS:
- #include is like copy-pasting the header's contents into the file that includes it
- The compiler ONLY processes files pulled into the build via #include or add_executable
- A header sitting on disk is just a text file — compiler never sees it until something includes it
- You can have a syntax error in a header and the build succeeds if nothing includes it
- Always include your headers in a .cpp file to actually get them compiled and checked

COMPILER vs IDE — HOW THEY FIND HEADERS:
- These are TWO separate systems that both need to know where your headers are
- The COMPILER (invoked by make) uses CMakeLists.txt — target_include_directories tells it where to look
  If make builds cleanly, the compiler found everything it needed
- The IDE (VS Code etc.) has its own separate header resolution system for features like
  autocomplete, error highlighting, and go-to-definition
  It does NOT automatically read CMakeLists.txt unless configured to do so
- An IDE include error does NOT mean your code won't compile — always check make first
- Fix: use the CMake Tools extension in VS Code to let the IDE read your CMakeLists.txt
  This syncs the IDE's understanding with the actual build system

STACK vs HEAP:
- Stack: automatic memory, where local variables and function calls live
  - Fixed, limited size
  - Memory freed automatically when variable goes out of scope
  - In embedded: blowing the stack (deep recursion, large locals) = crash, no OS to save you
- Heap: intentional memory, requested with 'new', must be freed with 'delete'
  - You ask for it, you are responsible for giving it back
  - Unpredictable timing (bad for real time systems)

MEMORY LEAK:
- When heap memory is allocated but never freed
- On a desktop app: OS reclaims memory when program exits
- On embedded system running 24/7: slowly consumes all RAM until system crashes
- On bare metal embedded (no OS): leaked memory is gone until hardware is power cycled
  No OS means no cleanup — ever. A leak during a mission = potential mission failure
- Every new MUST have a matching delete — embedded engineers are paranoid about this

OFF BY ONE ERROR:
- Classic bug with array loops — going one index too far
- m_size = 3 means valid indexes are 0, 1, 2
  i < m_size  -> i = 0, 1, 2        <- correct
  i <= m_size -> i = 0, 1, 2, 3     <- reads memory you don't own = undefined behavior
- Always use < not <= when iterating over array elements

WHAT delete[] ACTUALLY DOES:
- Runs the destructor on each element (if T has one)
- Returns the memory to the heap — marks those shelves as available again
- Does NOT touch the pointer itself — pointer still holds the old address after delete
- That's called a DANGLING POINTER — holds an address to memory you no longer own
- Always reassign or set to nullptr after deleting to avoid dangling pointers

DELETE ORDER MATTERS — avoid losing your only pointer:
- Wrong order:
    m_data = newData;   // old memory address lost forever = MEMORY LEAK
    delete[] newData;   // deletes the new memory you just assigned to m_data!
- Right order:
    delete[] m_data;    // free old memory first while you still have the address
    m_data = newData;   // now safely point to new memory
- Cutting off the only pointer to heap memory = that memory is leaked forever

RAII (Resource Acquisition Is Initialization):
- Tying a resource (like heap memory) to an object's lifetime
- When the object goes out of scope, the destructor cleans up the resource automatically
- Prevents memory leaks by making cleanup automatic

UNDEFINED BEHAVIOR:
- Uninitialized pointer pointing at random memory = ticking time bomb
- Always initialize pointers to nullptr if not pointing at anything yet

NULLPTR:
- Explicit way to say "this pointer points at nothing"
- Always use instead of leaving a pointer uninitialized

SIZE_T:
- Unsigned integer type designed for representing sizes and counts
- Can't be negative (a negative size makes no sense)
- Automatically matches the platform size (32 bit system = 32 bit size_t, etc.)
- Lives in <cstddef>
- Use for: size, capacity, array indices, loop counters over collections

FIXED WIDTH TYPES (from <cstdint>):
- uint32_t, uint16_t, int8_t etc.
- Guarantee exact size regardless of platform
- Critical in embedded systems where hardware varies and resources are scarce
- Seen a lot at Lockheed — now I know why

DECLARATION vs DEFINITION:
- Declaration: telling the compiler "this thing exists, here's its name and shape"
- Definition: the actual implementation, the body of the code

TEMPLATES:
- Allow a class or function to work with any type
- Syntax: template<typename T> above the class or function
- 'typename' and 'class' are interchangeable in this context (typename is more modern)
- T is used wherever a type would normally go
- DynamicArray<T> alone is NOT valid — T is unknown, compiler can't compile it
- Only when you provide a type (DynamicArray<int>) does the compiler generate real code
- The compiler generates a new CONCRETE CLASS for each type you use
  ex: DynamicArray<int>, DynamicArray<float>, DynamicArray<MyStruct> are all
  different generated classes under the hood
- For templated classes: both declaration AND definition go in the header file
- WHY: compiler needs the full definition visible when it sees DynamicArray<int>
  in main.cpp — if definition was in a .cpp file, compiler would never see it
- Regular classes can split declaration (.hpp) and definition (.cpp)
  Templates cannot — they live entirely in headers

RULE OF THREE / RULE OF FIVE:
- If you manage raw memory and need to define ANY of these, define ALL of them:

  RULE OF THREE (C++03 and earlier):
  1. Destructor          — clean up heap memory when object goes out of scope
  2. Copy constructor    — what happens when you copy the object
  3. Copy assignment     — what happens when you assign one object to another

  RULE OF FIVE (modern C++11 and later, adds):
  4. Move constructor    — transfer ownership instead of copying (more efficient)
  5. Move assignment     — same but for assignment

- WHY: if you manage raw memory, compiler-generated versions do the WRONG thing
  (shallow copy — copies the pointer, not the data — two objects pointing at same memory)
- We are writing embedded-style C++ so Rule of Three is the priority
- Rule of Five adds move semantics which are a modern C++ optimization

C++ VERSIONS AND EMBEDDED STYLE:
- C++03 and earlier: Rule of Three (copy only)
- C++11 and later (modern C++): Rule of Five — adds move semantics
- Move semantics: transfer OWNERSHIP of heap memory instead of copying it
  Instead of allocating new memory and copying all elements, just hand over the pointer
  Much more efficient than copying
- We write modern C++ (C++11+) but in EMBEDDED STYLE:
  Prioritize explicit control and determinism over convenience features
  Rule of Three first, move semantics later

DESTRUCTOR:
- Runs automatically when object goes out of scope
- Responsible for freeing heap memory the object owns
- Without it: m_data is never deleted = memory leak every time DynamicArray is destroyed
- Syntax: ~ClassName() { }

CONCRETE vs ABSTRACT CLASS (OOP):
- Abstract class: has at least one pure virtual function, cannot be instantiated
  defines an interface that subclasses MUST implement
- Concrete class: fully implemented, no unimplemented methods, CAN be instantiated
- In template context: "concrete" means a real fully-typed compilable class
  as opposed to the abstract template DynamicArray<T>

EXCEPTIONS IN EMBEDDED SYSTEMS:
- Exceptions use stack unwinding — runtime unwinds the call stack cleaning up objects
- Stack unwinding takes unpredictable time = breaks determinism
- Many embedded compilers disable exceptions entirely to save space and avoid overhead
- Some real time operating systems don't support them at all
- Rule: exceptions are usually off the table in embedded

ERROR HANDLING OPTIONS IN EMBEDDED (in place of exceptions):
1. assert(condition) — halts program immediately if condition is false
   - Debug builds: active, catches bugs early
   - Release builds: NDEBUG flag defined, assert compiles to nothing (no-op)
   - Same code, different behavior depending on build mode
   - Use for: catching programmer errors, invalid states that should never happen
2. Error codes — return a status, pass value out through a reference parameter
   - Common in production embedded code
   - Seen at Lockheed in logging systems
3. Return default constructed T{} — works for any type but caller can't distinguish error from real data
4. Return pointer — nullptr on failure, valid pointer on success

BEST PRACTICE IN EMBEDDED:
- assert() in debug builds to catch bugs during development
- Error codes in production for recoverable errors
- Never exceptions

ASSERT SYNTAX:
  #include <cassert>
  assert(condition that should be TRUE);
  // if condition is false: program halts immediately and reports where
  // example: assert(index < m_size);

PREPROCESSOR DIRECTIVES & BUILD MODES:
- NDEBUG (No Debug) — compiler flag set in release/production builds
- assert() checks NDEBUG automatically:
    debug build:   assert is active
    release build: assert compiles to nothing (zero cost)
- Same source code behaves differently based on build configuration

LAZY vs EAGER INITIALIZATION:
- Lazy: don't allocate resources until you actually need them
  - Better for real time embedded: scarce resources, unknown hardware
  - Let the engineer decide capacity — they know the hardware
- Eager: allocate upfront, ready to go immediately
  - Convenient but wastes resources if you never use them

================================================================================
REAL TIME EMBEDDED SYSTEMS
================================================================================

FIRMWARE:
- Firmware IS real software — not "in between" hardware and software, that's imprecise
- Software written specifically to control hardware directly
- Stored in non-volatile memory (flash memory) on the chip itself
- Tightly coupled to specific hardware — not portable like application software
- Firmware engineers write C and C++ — real code, real logic, real bugs
- Likely what you'd be writing at the new role

PROGRAM vs SOFTWARE vs FIRMWARE:
- Program: a specific executable, one thing that runs
- Software: broader term, collection of programs, libraries, a whole system
- Firmware: software burned onto hardware, runs without an OS

WHY "BURNED":
- Old term from ROM chips (Read Only Memory) — data written using a ROM burner device
  using electrical signals, permanent and non-erasable
- Modern chips use FLASH memory — erasable and rewritable
- "Burned" stuck around out of habit, still used constantly in the industry

MEMORY LEAK DETECTION TOOLS:
- Valgrind: runs your program through it, tracks every allocation, reports what was never freed
  Common on Mac/Linux
- AddressSanitizer: built into clang, add a compiler flag and it instruments your code automatically
  Easier to use than Valgrind on Mac
  We will use this in the performance section

new[] vs delete[] vs new vs delete:
- ALWAYS match: new[] with delete[], and new with delete
- T* pointer doesn't tell you if it points to one element or an array — YOU have to remember
- Using delete instead of delete[] on an array = undefined behavior, only destroys first element
- The compiler will NOT catch this mismatch

================================================================================

EMBEDDED:
- Software running on dedicated hardware with a specific purpose
- Not a general purpose computer
- Examples: microcontrollers, FPGAs, sensors, guidance systems
- Often: limited RAM, no hard drive, sometimes no OS at all

REAL TIME:
- Software has TIMING GUARANTEES
- Not just "fast" — operations MUST complete within X microseconds, every time
- Missing a deadline = correctness failure, not just a performance problem
- Example: guidance system that calculates course correction 10ms too late is WRONG

WHY STL IS OFTEN AVOIDED IN EMBEDDED:
- std::vector, std::string etc. use dynamic memory allocation internally
- Dynamic allocation has unpredictable timing — unacceptable in real time systems
- STL overhead can be too large for resource constrained hardware
- Some embedded environments don't have full STL available
- Solution: roll your own — know exactly what the code does and how long it takes

================================================================================
PROJECT: LOW LEVEL DATA STRUCTURES LIBRARY
================================================================================

GOAL: Build a reusable data structures library without STL, from scratch
      Simulates what real embedded engineers actually write

ROADMAP:
  1. Dynamic Array        <- currently building (memory + pointers + templates)
  2. Linked List          (pointers + structs)
  3. Queue or Stack       (built on top of linked list)
  4. Bit flags            (bit manipulation)
  5. Thread safety        (concurrency)
  6. Profiling            (performance + Big O in practice)

LEARNING STRUCTURE:
  - After each concept: explain it back in my own words
  - After each feature: small coding challenge without help
  - Every few topics: mini review session revisiting older concepts

POINTERS AND ARRAY INDEXING UNDER THE HOOD:
- A pointer holds the address of the FIRST element only
- The compiler finds other elements using math:
    address of element[i] = base address + (i * sizeof(T))
- When you write newData[2], the compiler turns that into:
    "go to base address, skip forward 2 elements worth of bytes, read what's there"
- This is why arrays MUST be contiguous — the math only works if elements are next to each other
- T* looks the same for a pointer to a single element AND a pointer to an array
  The pointer is just an address — context tells you how to use it

MENTAL MODEL — HEAP MEMORY AS A WAREHOUSE:
- Heap memory is like a giant warehouse with numbered shelves
- When you call: T* newData = new T[4];
  You're saying: "Give me 4 shelves in a row, give me the address of the first one"
- newData holds that address (e.g. shelf 1042)
- The array lives at shelves 1042, 1043, 1044, 1045 — contiguous, all in a row
- A pointer holds the address of the FIRST shelf — the rest follow sequentially
- During a grow operation:
    m_data  -> shelf 500:  [A, B, C, C]              <- old chunk, full
    newData -> shelf 1042: [_, _, _, _, _, _, _, _]   <- new chunk, empty
  Then copy old into new, delete old, point m_data at new

DYNAMIC ARRAY — Member Variables:
  T* data          <- pointer to heap memory block where elements live
  size_t size      <- how many elements are currently stored
  size_t capacity  <- how much space is allocated

DYNAMIC ARRAY — Growth Strategy:
  When full:
  1. Allocate new larger chunk of heap memory
  2. Copy all existing elements to new chunk
  3. Delete old chunk
  4. Point data pointer at new chunk
  5. Update capacity
  - Can't extend existing chunk — something else might be adjacent in memory
  - Copy step is O(n) — expensive, real time engineers think carefully about this

================================================================================
GIT & GITHUB
================================================================================

THREE STEP WORKFLOW:
  git add <files>    <- stage specific files (be explicit, not git add .)
  git commit -m ""   <- commit with a message
  git push           <- push to remote (origin)

WHY BE EXPLICIT WITH git add:
- git add . adds everything including secrets, credentials, large binaries
- Being explicit means consciously choosing what goes in each commit
- Especially important on defense/sensitive projects

ATOMIC COMMITS:
- Each commit should represent one logical change
- Makes history readable, easy to revert specific changes, easier to review in PRs

COMMIT MESSAGE CONVENTION:
  First line: short summary, under 50 chars, imperative tense ("Add" not "Added")
  Blank line
  Body: longer description if needed

.gitignore:
- Controls what git ignores and won't track
- Add generated/build folders (Build/), secrets, credentials
- The .gitignore file itself SHOULD be committed so teammates get it too

REMOTE:
- origin — default name for the remote repository on GitHub
- git remote add origin <url> — connects local repo to GitHub
- git push -u origin main — pushes and sets upstream tracking

BRANCHING STRATEGY (real engineer workflow):
- Never commit directly to main — it should always be stable and buildable
- Create a feature branch for each piece of work, merge back when done
- Convention for this project:
    main                    <- stable, always builds
    feature/dynamic-array   <- current work
    feature/linked-list     <- next data structure
    etc.

BRANCH COMMANDS:
  git checkout -b <branch-name>   <- classic way: create and switch in one command
  git switch -c <branch-name>     <- newer, more readable way (does the same thing)
  git switch <branch-name>        <- switch to an existing branch (no -c)
  git branch                      <- list all local branches

  -c stands for CREATE — switch alone just switches, -c creates first then switches

PUSHING A NEW BRANCH TO GITHUB:
  git push -u origin <branch-name>
  -u sets up tracking so future 'git push' knows where to send it
  Only need -u the first time — after that just 'git push'

================================================================================
PROGRAM DESIGN & STRUCTURE (to cover as project grows)
================================================================================
- Currently a learning gap — struggle with how objects connect without spaghetti code
- Recently learned: dependency injection (need more reps)

CONCEPTS TO LEARN IN CONTEXT THROUGH THE PROJECT:
- Encapsulation: how does an object expose itself safely to outside code?
- Dependency management: how do you use one object inside another without tight coupling?
- Separation of concerns: adding new things shouldn't break existing things
- Dependency injection: passing dependencies in rather than creating them internally
- SOLID principles
- Design patterns

NOTE: Learn these in context as the library grows, not from a textbook

================================================================================
BIG O NOTATION (intro)
================================================================================
- Language for describing how an algorithm SCALES as input grows
- Not exact speed — describes the relationship between input size and work done
- Covers both TIME complexity and SPACE (memory) complexity

O(1)     — constant time, doesn't matter how big input is (ex: array index lookup)
O(n)     — linear, scales with input size (ex: copying all elements)
O(n²)   — quadratic, nested loops, gets bad fast
O(log n) — logarithmic, think binary search, very efficient

================================================================================
